{
  "name": "trustvault-credit",
  "version": "0.1.0",
  "private": true,
  "description": "Programmable working capital for autonomous agents",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "cd packages/credit-worker && npm run dev",
    "deploy": "cd packages/credit-worker && npm run deploy",
    "typecheck": "tsc --noEmit -p packages/credit-worker/tsconfig.json"
  },
  "devDependencies": {
    "solc": "^0.8.24",
    "tsx": "^4.21.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.14.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0",
    "viem": "^2.47.6"
  }
}
