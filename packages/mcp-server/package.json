{
  "name": "credmesh-mcp-server",
  "version": "0.1.0",
  "private": true,
  "description": "MCP server for CredMesh — makes agent credit infrastructure discoverable and callable by any MCP-compatible AI agent",
  "type": "module",
  "bin": {
    "credmesh-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  }
}
